/**
 * LendIT Configuration
 * Exposing global settings without enforcing ES modules
 */

window.FEATURES = {
    examPass: false,
};

window.UI = {
    toastDuration: 4000,
};

window.LIMITS = {
    maxPhotos: 5,
    maxChatLength: 500,
};
