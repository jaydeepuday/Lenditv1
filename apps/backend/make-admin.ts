import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const email = process.argv[2]; if (!email) { console.log('Please provide an email. Example: npx ts-node make-admin.ts user@woxsen.edu.in'); process.exit(1); } const user = await prisma.user.update({ where: { email }, data: { role: 'ADMIN' } }); console.log('Successfully made ' + user.email + ' an ADMIN!'); } main().catch(console.error).finally(() => prisma.$disconnect());
