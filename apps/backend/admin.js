const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({ datasources: { db: { url: "postgresql://postgres:postgres@localhost:5432/lendit_db?schema=public" } } }); async function run() { await prisma.user.update({ where: { email: 'jaydeep.g_2029@woxsen.edu.in' }, data: { role: 'ADMIN' } }); console.log('SUCCESS!'); } run().catch(console.error).finally(() => prisma.$disconnect());
